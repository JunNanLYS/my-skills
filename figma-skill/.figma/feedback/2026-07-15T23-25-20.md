# figma-skill v2.2 Self-Reflection
<!-- skill-version: 2.2 -->

## 1. 问题列表 (Problems)

| # | 问题 | 出现的 Workflow | 影响 |
| - | ---- | ---------------- | ---- |
| 1 | `runFigmaScript` helper 返回已 parse 的对象而非 string，inline 写测试时容易二次 `JSON.parse` 导致 `[object Object]`。Plan 模板的 fixture 1-4 写错（再 `JSON.parse(runFigmaScript(...))`），需要读 helper 实现确认返回类型。 | Plan review / Task 2 | 第一个实现失败，debug 5 分钟 |
| 2 | `overlap-check.mjs` 当前 fixture 5 frame mock 字段名写成了 `bbox` 而非 `absoluteBoundingBox`，导致 `parent.absoluteBoundingBox` undefined → throw。 | Plan review / Task 2 | 第二个实现失败，debug 2 分钟 |
| 3 | workflow-contract.test.mjs 与 task-state-cli.test.mjs 都 hard-code `version: 2.1` 与 `skill-version: 2.1`，升级 SKILL.md 到 v2.2 后两个测试 fail。Plan 没说升级时同步改这两个测试。 | Task 4 / Task 9 | 1 测试 fail，回归时才发现要同步改 |
| 4 | Plan Task 1 假设把 `clipWhitelist` 加进 `task-state.schema.json`，但 schema `additionalProperties: false` 且无 `plan` 顶级字段。Plan 设计与代码现状不匹配。 | Plan review | 必须修订 plan Task 1 为 `assertValidPlan` helper（新增 commit `5a55679`） |
| 5 | `figma-task-state.mjs reflect` 当前只生成占位模板，不接受 `--content` 或 `--problems-file` 等填充参数；agent 必须 Edit 文件填内容。如果忘记填就是 `SelfReflectionGate=FAIL`，但没有 nudge 机制。 | Workflow 12 实战 | 第一次实战发现"工具 + 流程"中间缺一步 |
| 6 | Inline 执行模式（executing-plans）在 main 分支直接落地，10 个 task × commit cadence 都在 main 上累积；按 executing-plans skill "Never start implementation on main/master branch without explicit user consent" 规则需要先确认。 | Plan execution | 主分支历史被 v2.2 占满，但用户明确选 Inline |
| 7 | `.figma/feedback/<timestamp>.md` 现在是"落盘即结束"，没有反馈到 SKILL.md 的机制（暂缓 P2-7 之后）。第一份实战自省写完后无下游消费路径。 | Workflow 12 文档 | 长期风险：自省堆积成档案柜，无人翻 |

## 2. 优化方向 (Optimization Directions)

| # | 优化方向 | 优先级 | 关联问题 |
| - | -------- | ------ | -------- |
| 1 | `figma-task-state.mjs reflect` 新增 `--content-file <path>` 或 `--content -` (stdin)，让 agent 在同一次调用里填完两表内容；保留 skill-version 校验 + 敏感数据过滤。 | P1 | 问题 #5 |
| 2 | Plan 模板（含 writing-plans skill 输出）在每个会改 SKILL.md version 的 Task 旁加"同步更新 tests 中 hard-code version 的位置"清单；或提取到 `tests/.version-refs.txt` 让单一来源管理。 | P1 | 问题 #3 |
| 3 | 给 `runFigmaScript` helper 加 JSDoc 明确返回类型（"已 parse 的对象"），并在 planning.md "Required Fields Quick Map" 加一行"测试 helper 返回类型"。 | P2 | 问题 #1 |
| 4 | `overlap-check.mjs` 与 `overlap-check.test.mjs` 之间加"mock 字段名表"，列出 frame/child 必备字段（id/name/clipsContent/absoluteBoundingBox/children），fixture 模板化。 | P2 | 问题 #2 |
| 5 | brainstorming / writing-plans skill 启动时强制"探测现有 schema 与代码"，列出"已知 schema 字段"、"已知 helper 签名"、"已知测试返回类型"——避免 plan 设计与现实脱节。 | P1 | 问题 #1, #2, #4 |
| 6 | executing-plans skill 在主分支执行时，**第一步**自动提醒"建议建 worktree"，并 call EnterWorktree 而不是直接干。 | P2 | 问题 #6 |
| 7 | 暂缓 P2-7 v0.2（自省反馈 cadence）——等本次自省实战数据 + 后续几次自省后再设计阈值（≥ 5 份 / 30 天）。问题 #7 的优化方向必须**有数据驱动**而非凭空定阈值。 | P2 | 问题 #7 |
| 8 | **本次实战发现**：`figma-skill/.figma/` 被仓库根级 `.gitignore` line 7 `.figma/` 整体排除——意味着 Workflow 12 生成的 `.figma/feedback/<timestamp>.md` **无法 commit 到 git**，违背 v2.1 spec §"all .figma/ files are eligible for Git tracking" 声明。验证：`git ls-files | grep .figma` 返回 0 行；同时 `.figma/tasks/<id>/state.json` 也未入 git——v2.0 设计文档与实际 gitignore 矛盾。 | Workflow 12 实战 | 自省文件无 git 跨会话追溯；任务账本状态机恢复依赖本地文件系统而非 git |
